{
    "name": "axi4lite_reg_generator",
    "image": "mcr.microsoft.com/devcontainers/python:3",
    "settings": {
        "terminal.integrated.shell.linux": "/bin/bash"
    },
    "extensions": [
        "ms-python.python",
        "ms-python.vscode-pylance",
        "puorc.awesome-vhdl",
        "mshr-h.veriloghdl"
    ],
    "containerEnv": {
        "COCOTB_IGNORE_PYTHON_REQUIRES": "1"
    },
    "postCreateCommand": "sudo apt-get update && sudo apt-get install -y verilator ghdl-gcc && pipx install uv && uv sync --extra dev --extra rtl && uv run pre-commit install"
}
