{
    "name": "axi4lite_reg_generator",
    "build": {
        "dockerfile": "../Dockerfile"
    },
    "settings": {
        "terminal.integrated.shell.linux": "/bin/bash"
    },
    "extensions": [
        "ms-python.python",
        "ms-python.vscode-pylance",
        "puorc.awesome-vhdl"
    ],
    "postCreateCommand": "apt install -y git && poetry install --no-interaction && poetry run pre-commit install"
}
